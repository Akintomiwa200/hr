# DATABASE_URL=
# "postgresql://neondb_owner:npg_8q4WbMoNDTih@ep-purple-shadow-aq7augl5-pooler.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
JWT_SECRET="smart-hr-dev-secret-change-in-production"

# App URL used in welcome emails (login links)
APP_URL="http://localhost:3000"

# Check-in device / kiosk API key (optional master key for all devices)
ATTENDANCE_DEVICE_API_KEY="dev-attendance-device-master-key"

# SMTP — required for real inbox delivery (Gmail, Outlook, SendGrid, etc.)
# Without these, dev mode uses Ethereal test mail with a preview link in the API response.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=herkintormiwer@gmail.com
SMTP_PASS=ddthgkpwjzptedhy
SMTP_FROM="Smart HR <your-email@gmail.com>"



# Google Calendar & Meet (OAuth) — for interview scheduling
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
# Must match Authorized redirect URIs in Google Cloud Console exactly.
# Default (if omitted): {APP_URL}/api/google/callback
GOOGLE_REDIRECT_URI="http://localhost:3000/api/google/callback"

# Zoho OAuth — People, Recruit, Books, Sign, Mail
# ZOHO_CLIENT_ID=
# ZOHO_CLIENT_SECRET=
# ZOHO_ACCOUNTS_URL="https://accounts.zoho.com"
# ZOHO_API_DOMAIN="https://www.zohoapis.com"
# Must match Authorized Redirect URI on the Zoho API client exactly.
# ZOHO_REDIRECT_URI="http://localhost:3000/api/integrations/zoho/callback"



# Optional: protect POST /api/integrations/cron fallback polling
INTEGRATION_CRON_SECRET="change-me-in-production"

# cloudinary — used for checklist task files, attendance photos, and
# attendance month archives (free primary-storage space on completed months).
# Provide either CLOUDINARY_URL or the individual credentials below.
CLOUDINARY_URL="cloudinary://<your_api_key>:<your_api_secret>@dcojm0hqf"
CLOUDINARY_CLOUD_NAME="Root"
CLOUDINARY_API_KEY="174564861314216"
CLOUDINARY_API_SECRET="i1Zg8lmpVoUDY9X_RMejlikQm58"
